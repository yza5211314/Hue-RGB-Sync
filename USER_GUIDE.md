# 🎮 RGB键盘控制系统使用指南

## ✅ 功能完成状态

### 已实现功能
- ✅ **单键独立控制** - 点击虚拟键盘上的任意按键，只改变该按键的颜色
- ✅ **全键盘统一设置** - 一键应用颜色到所有区域
- ✅ **虚拟键盘显示** - 标准104键布局可视化
- ✅ **实时颜色同步** - 集成 RGB 滑块、快捷色块、颜色选择器
- ✅ **屏幕取色同步** - 键盘颜色跟随屏幕内容变化
- ✅ **自动设备连接** - GUI启动时自动扫描并连接设备
- ✅ **内置模式切换** - 支持呼吸、波浪、光谱等 6 种硬件灯效

---

## 🚀 快速开始

### 1. 启动程序

```bash
# 方式1: 双击批处理文件（推荐）
启动控制面板.bat

# 方式2: 命令行运行
python gui_control.py
```

### 2. 以管理员权限运行

**重要**: 必须以管理员权限运行才能控制键盘！

```
右键点击 "启动控制面板.bat"
选择 "以管理员身份运行"
```

### 3. 检查设备连接

GUI启动后会自动扫描设备，日志区域会显示：
```
[HH:MM:SS] ✅ mk850 连接成功
```

---

## 🎨 使用方法

### 方法1: 单键独立控制 

**步骤**:
1. 在颜色控制区选择颜色（RGB滑块或预设按钮）
2. 在虚拟键盘上点击**单个按键**
3. 实体键盘对应按键亮起该颜色
4. 其他按键保持原色不变

**示例**:
```
1. 设置RGB为 (255, 0, 0) 红色
2. 点击虚拟键盘的 "W" 键
3. 实体键盘只有 W 键变红
4. 其他按键颜色不变
```

### 方法2: 全键盘统一颜色

**步骤**:
1. 选择喜欢的颜色
2. 点击 "✅ 应用到所有设备" 按钮
3. 整个键盘变为统一颜色

### 方法3: 预设颜色

**步骤**:
1. 点击预设按钮（⚪白、⚫黑、红、🟢绿、🔵蓝）
2. 整个键盘立即变为该颜色

### 方法4: 屏幕取色同步

**步骤**:
1. 点击 "▶️ 启动同步" 按钮
2. 键盘颜色会自动跟随屏幕中心区域的颜色
3. 点击 "⏹️ 停止同步" 停止

---

## 🎮 应用场景

### 场景1: 游戏WASD键高亮

```
操作:
1. 选择红色 (255, 0, 0)
2. 依次点击 W, A, S, D
3. 只有WASD四个键是红色

效果:
游戏中的移动键被醒目高亮 
```

### 场景2: 快捷键分区配色

```
WASD (移动键): 红色
QERF (技能键): 蓝色
1234 (物品栏): 绿色
SPACE (跳跃): 黄色
CTRL (蹲下): 紫色

效果:
不同功能区的按键使用不同颜色，一目了然！
```

### 场景3: 编程工作配色

```
CTRL/ALT (快捷键修饰键): 蓝色
TAB (缩进): 绿色
ENTER (执行): 红色
ESC (退出): 黄色

效果:
常用快捷键醒目高亮，提高工作效率 ️
```

---

## 🔧 故障排除

### 问题1: 键盘没有反应

**症状**: 点击虚拟按键，GUI显示变了但键盘硬件不变

**解决方案**:
1. ✅ **确认以管理员权限运行**
   - 右键 → "以管理员身份运行"
   
2. ✅ **检查键盘连接**
   - 查看日志是否有 "✅ mk850 连接成功"
   - 如果没有，点击 "🔍 扫描设备" 按钮
   
3. ✅ **关闭OpenRGB**
   - 确保OpenRGB软件已完全关闭
   - 两个程序不能同时控制键盘
   
4. ✅ **检查USB连接**
   - 确认键盘USB线已插入
   - 尝试更换USB端口

### 问题3: 颜色不准确

**症状**: 设置的颜色与实际显示不符

**解决方案**:
1. 尝试调整RGB值
2. 检查键盘是否处于正确的模式（静态模式）
3. 确认键盘固件版本

---

##  技术信息

### 按键映射

- **总映射按键**: 105个
- **键盘布局**: 标准104键
- **支持情况**: 已完整覆盖标准104键及多媒体增强按键

### 通信协议

- **HID接口**: 用法页 0xFF1C
- **Report ID**: 0x04
- **命令码**:
  - 0x01: Begin（开始接收数据）
  - 0x02: End（应用数据）
  - 0x11: 写入颜色数据
- **数据包大小**: 64字节
- **每包颜色数据**: 最多18个按键（54字节）

### 按键索引逻辑
系统采用槽位映射机制，索引范围为 `0 - 124`：
- **0-20**: ESC 及 F 功能键区
- **21-41**: 主键区数字行及部分小键盘控制键
- **42-83**: 主键区字母行及小键盘数字区
- **84-124**: 修饰键、方向键及完整小键盘区
*具体对应关系请参考 `mk850_key_mapping.ini` 配置文件。*

---

##  更新日志

### v2.1.0 (当前版本)
- ✅ 升级至 105 键全键独立控制
- ✅ 优化 HID 通信稳定性，降低同步延迟
- ✅ 完善 GUI 紧凑型布局
- ✅ 同步更新文档与依赖说明

### v2.0.4
- ✅ 修复键盘单键控制功能
- ✅ 修复Begin/End命令格式
- ✅ 添加校验和计算
- ✅ 增强调试输出
- ✅ GUI启动时自动连接设备
- ✅ 添加详细日志信息

### v2.0.3
- ✅ 添加虚拟键盘可视化
- ✅ 支持点击按键控制

### v2.0.2
- ✅ 修复HID接口选择问题
- ✅ 清理测试文件

---

## 💡 提示

1. **首次使用**: 务必以管理员权限运行
2. **保存配色**: 可以截图保存你的配色方案
3. **性能**: 单键控制延迟 <50ms

---

##  技术支持

如遇到问题，请检查：
1. 日志区域的错误信息
2. 是否以管理员权限运行
3. OpenRGB是否已关闭
4. USB连接是否正常

---

**版本**: v2.1.0  
**更新日期**: 2026-05-10  
**状态**: ✅ 生产就绪